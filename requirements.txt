# Medical Device Prototype - Python Dependencies
# Spine Cooling Runtime
#
# Works on Windows/macOS for --sim, and on Raspberry Pi for real hardware.
# Packages with environment markers are skipped automatically off-Pi.

# ============================================================================
# CORE RUNTIME (UI / config — needed everywhere)
# ============================================================================

PyQt6>=6.4.0
PyYAML>=6.0

# ============================================================================
# HARDWARE (Raspberry Pi only — skipped on Windows/macOS)
# ============================================================================

RPi.GPIO>=0.7.1; sys_platform == "linux" and platform_machine in "aarch64|armv7l|armv6l|armv8l"
pigpio>=1.78; sys_platform == "linux"
smbus2>=0.4.0; sys_platform == "linux"
SMtc; sys_platform == "linux"
adafruit-blinka>=8.0.0; sys_platform == "linux"
adafruit-circuitpython-ads1x15>=2.4.0; sys_platform == "linux"

# ============================================================================
# OPTIONAL / TOOLS
# ============================================================================

# Offline analysis (tools/plot_temps.py, tools/pump_flow_fit.py)
pandas>=2.0
matplotlib>=3.7
numpy>=1.24

# UART simple_examples (uart_master / uart_slave / uart_listener)
pyserial>=3.5
